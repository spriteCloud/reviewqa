name: reviewqa

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      pr:
        description: PR number to run against
        required: true

permissions:
  contents: write
  pull-requests: write

jobs:
  reviewqa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: spriteCloud/reviewqa@v1
        with:
          pr: ${{ github.event.inputs.pr || github.event.pull_request.number }}
          openai-base-url: ${{ vars.OPENAI_BASE_URL || 'https://api.openai.com/v1' }}
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          model: ${{ vars.REVIEWQA_MODEL || 'gpt-4o-mini' }}
          heal-mode: on-failure
          playwright-report: playwright-report.json
          log-level: info
          # Optional probe target — when set, reviewqa generate ALSO fetches
          # the URL and emits a Playwright happy-flow against it.
          target-urls: ${{ vars.REVIEWQA_TARGET_URLS }}
