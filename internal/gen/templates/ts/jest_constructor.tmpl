// Generated by reviewqa.
//
// Constructor smoke for `{{.Symbol.Name}}`. Builds an instance with
// default values for each declared parameter and asserts:
//   1. Construction does not throw
//   2. The resulting instance carries the same field values
//
// Filter: `npx jest --testPathPattern .constructor.test.ts`
import { describe, it, expect } from '@jest/globals'
import { {{.Symbol.Name}} } from '{{.ImportPath}}'

describe('{{.Symbol.Name}} — constructor @kind:constructor', () => {
  it('@kind:constructor builds with valid args and exposes them', () => {
    const instance = new {{.Symbol.Name}}(
      {{- range $i, $p := .Symbol.Params}}{{if $i}}, {{end}}{{dtoSampleValue $p.Type}}{{- end}}
    )
    expect(instance).toBeDefined()
{{- range .Symbol.Params }}
    // Field {{.Name}} should be a property on the constructed instance.
    expect((instance as any).{{.Name}}).toBeDefined()
{{- end }}
  })
})
