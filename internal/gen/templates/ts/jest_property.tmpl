// Generated by quail.
//
// Property-based test stub for `{{.Symbol.Name}}`. fast-check generates
// arbitrary inputs and asserts the function doesn't throw — a baseline
// invariant for any pure function. Replace the no-throw assertion with
// the documented invariant when you have one (e.g. "sort output is
// ordered", "encode/decode is identity").
//
// Filter: `npx jest --testPathPattern .property.test.ts`
import fc from 'fast-check'
import { {{.Symbol.Name}} } from '{{.ImportPath}}'

describe('{{.Symbol.Name}} — property-based', () => {
  it('@kind:property does not throw on arbitrary inputs', () => {
    fc.assert(
      fc.property(
        {{- range $i, $p := .Symbol.Params }}
        {{- if $i}},{{end}}
        {{propertyArbitraryFor $p}}
        {{- end }}
        ,
        ({{- range $i, $p := .Symbol.Params}}{{if $i}}, {{end}}{{$p.Name}}{{- end}}) => {
          expect(() => {{.Symbol.Name}}({{- range $i, $p := .Symbol.Params}}{{if $i}}, {{end}}{{$p.Name}}{{- end}})).not.toThrow()
        }
      ),
      { numRuns: 100 }
    )
  })
})
