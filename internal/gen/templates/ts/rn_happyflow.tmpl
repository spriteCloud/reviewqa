// Generated by quail for React Native component `{{.Symbol.Name}}`.
//
// Detox happy-flow scaffold: mount the screen, assert primary
// elements render, perform the deterministic interaction implied by
// the component shape (button tap, text input).
//
// Filter: `detox test --testPathPattern .detox.test.ts`
import { device, element, by, expect as detoxExpect } from 'detox'

describe('{{.Symbol.Name}} — Detox happy flow @kind:mobile-rn', () => {
  beforeAll(async () => {
    await device.launchApp({ newInstance: true })
  })

  it('@kind:mobile-rn renders the screen', async () => {
    await detoxExpect(element(by.id('{{.Symbol.Name | lower}}-screen'))).toBeVisible()
  })
})
