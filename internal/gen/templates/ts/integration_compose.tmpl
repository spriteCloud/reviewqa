# Generated by quail from quail.yml.
# docker-compose.test.yml — bring up the resource stack with
#   docker compose -f docker-compose.test.yml up -d
# then run the integration suite.
version: "3.9"
services:
{{- range $.Integration.Containers.Databases }}
  {{.Name}}:
    image: {{.Image}}
    environment:
      POSTGRES_PASSWORD: quail
      POSTGRES_USER: quail
      POSTGRES_DB: {{.Name}}
{{- end }}
{{- range $.Integration.Containers.Brokers }}
  {{.Kind}}:
    image: {{.Image}}
{{- end }}
{{- range $.Integration.Containers.Caches }}
  {{.Kind}}:
    image: {{.Image}}
{{- end }}
