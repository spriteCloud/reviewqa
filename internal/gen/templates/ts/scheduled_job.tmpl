// Generated by reviewqa.
//
// Scheduled-job smoke for `{{.Symbol.Name}}`. Invokes the job
// directly and asserts it runs to completion without throwing. The
// trigger logic (cron string, decorator, framework-specific scheduler)
// is out of scope here — what we assert is that the job's body, in
// isolation, doesn't immediately blow up on first call.
//
// Filter: `npx jest --testPathPattern .cron.test.ts`
import { describe, it, expect } from '@jest/globals'
import { {{.Symbol.Name}} } from '{{.ImportPath}}'

describe('{{.Symbol.Name}} — scheduled job @kind:scheduled', () => {
  it('@kind:scheduled invokes without throwing', async () => {
    await expect(Promise.resolve({{.Symbol.Name}}())).resolves.not.toThrow()
  })
})
