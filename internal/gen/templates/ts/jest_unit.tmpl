// Generated by reviewqa. Review and extend with edge cases.
import { {{.Symbol.Name}} } from '{{.ImportPath}}'

describe('{{.Symbol.Name}}', () => {
  it('returns a value for valid input', () => {
    const result = {{.Symbol.Name}}({{.HappyArgs}})
    expect(result).toBeDefined()
  })
{{- $n := .Symbol.Name }}
{{- if or (hasPrefix $n "is") (hasPrefix $n "has") (hasPrefix $n "should") (hasPrefix $n "can") }}

  it('returns a boolean', () => {
    const result = {{.Symbol.Name}}({{.HappyArgs}})
    expect(typeof result).toBe('boolean')
  })
{{- end }}
{{- if .Symbol.Params }}

  it.each([
    [{{.HappyArgs}}],
  ])('returns a value for arguments: %p', (...args) => {
    const result = ({{.Symbol.Name}} as any)(...args)
    expect(result).toBeDefined()
  })
{{- end }}
})
