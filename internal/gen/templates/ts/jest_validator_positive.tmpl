// Generated by quail.
//
// Validator positive-case test for `{{.Symbol.Name}}`. Asserts a
// deterministic valid input passes. Add negative cases by hand —
// they're context-dependent and out of scope for generation.
//
// Filter: `npx jest --testPathPattern .validator.test.ts`
import { {{.Symbol.Name}} } from '{{.ImportPath}}'

describe('{{.Symbol.Name}} — validator positive case', () => {
  it('@kind:validator accepts a deterministic valid input', () => {
    // Sample valid input — edit when your validator's contract expands.
    const validInput = {
{{- range $.Symbol.Params }}
      {{.Name}}: {{dtoSampleValue .Type}},
{{- end }}
    }
    expect(() => {{.Symbol.Name}}(validInput as any)).not.toThrow()
  })
})
