// Generated by quail from quail.yml.
//
// Shared Testcontainers manifest for the integration test suite.
// Each declared resource has its own *.integration.test.ts that
// reuses the helpers exported here.
import { GenericContainer } from 'testcontainers'

export const containers = {
{{- range $.Integration.Containers.Databases }}
  '{{.Name}}': () => new GenericContainer('{{.Image}}'),
{{- end }}
{{- range $.Integration.Containers.Brokers }}
  '{{.Kind}}': () => new GenericContainer('{{.Image}}'),
{{- end }}
{{- range $.Integration.Containers.Caches }}
  '{{.Kind}}': () => new GenericContainer('{{.Image}}'),
{{- end }}
}
