// Generated by quail.
//
// Email-sender smoke for `{{.Symbol.Name}}`. Calls the sender with a
// placeholder recipient and asserts no exception. Replace the
// transport with a fake / in-memory transport so this can run in CI
// without actually sending mail.
//
// Filter: `npx jest --testPathPattern .email.test.ts`
import { describe, it, expect } from '@jest/globals'
import { {{.Symbol.Name}} } from '{{.ImportPath}}'

describe('{{.Symbol.Name}} — email sender @kind:email', () => {
  it('@kind:email assembles + dispatches without throwing', async () => {
    const sample = { to: 'quail@example.test', subject: 'quail probe', body: 'placeholder' }
    await expect(Promise.resolve({{.Symbol.Name}}(sample as any))).resolves.not.toThrow()
  })
})
