// Generated by quail. Review and extend with edge cases.
import request from 'supertest'
import app from '{{.AppImportPath}}'

describe('{{.Symbol.Method}} {{.Symbol.Path}}', () => {
  it('responds with 2xx on a valid request', async () => {
    const res = await request(app).{{.SupertestMethod}}('{{.Symbol.Path}}')
    expect(res.status).toBeGreaterThanOrEqual(200)
    expect(res.status).toBeLessThan(300)
  })

  it('responds with a JSON content-type', async () => {
    const res = await request(app).{{.SupertestMethod}}('{{.Symbol.Path}}')
    expect(String(res.headers['content-type'] ?? '')).toMatch(/application\/json/)
  })

  it('returns a non-empty body', async () => {
    const res = await request(app).{{.SupertestMethod}}('{{.Symbol.Path}}')
    expect(res.body).toBeDefined()
    expect(JSON.stringify(res.body).length).toBeGreaterThan(2)
  })
})
