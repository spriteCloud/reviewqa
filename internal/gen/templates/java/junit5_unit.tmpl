// Generated by quail. Review and extend with edge cases.
package {{.Package}};

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class {{.Symbol.Receiver}}Test {

    @Test
    @DisplayName("{{.Symbol.Name}} returns a value on valid input")
    void {{.Symbol.Name}}_returnsValue() {
        {{.Symbol.Receiver}} subject = new {{.Symbol.Receiver}}();
{{- if isPrimitiveType .Symbol.Returns }}
        var result = subject.{{.Symbol.Name}}({{.HappyArgs}});
        assertNotEquals({{defaultForType .Symbol.Returns}}, result);
{{- else }}
        var result = subject.{{.Symbol.Name}}({{.HappyArgs}});
        assertNotNull(result);
{{- end }}
    }

    @Test
    @DisplayName("{{.Symbol.Name}} does not throw on valid input")
    void {{.Symbol.Name}}_doesNotThrow() {
        {{.Symbol.Receiver}} subject = new {{.Symbol.Receiver}}();
        assertDoesNotThrow(() -> subject.{{.Symbol.Name}}({{.HappyArgs}}));
    }
}
