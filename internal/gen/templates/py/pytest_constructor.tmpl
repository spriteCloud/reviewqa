# Generated by reviewqa.
#
# Constructor smoke for `{{.Symbol.Name}}`. Builds an instance with
# default values per parameter and asserts construction succeeds.
#
# Filter: `pytest -k constructor`
from {{.ImportPath}} import {{.Symbol.Name}}


def test_{{.SnakeName}}_constructs_with_valid_args():
    instance = {{.Symbol.Name}}(
{{- range $.Symbol.Params }}
        {{.Name}}={{dtoSampleValuePy .Type}},
{{- end }}
    )
    assert instance is not None
{{- range .Symbol.Params }}
    assert hasattr(instance, "{{.Name}}"), "missing field {{.Name}} on constructed instance"
{{- end }}
