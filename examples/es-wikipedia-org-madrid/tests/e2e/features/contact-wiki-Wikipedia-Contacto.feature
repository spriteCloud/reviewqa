# Generated by quail.
#
# Gherkin contract for the contact journey on https://es.wikipedia.org/wiki/Madrid.
#
# This .feature file is EXECUTED by playwright-bdd at test time.
# Step definitions live in `tests/e2e/steps/quail.steps.ts` and bind
# each Given/When/Then to the helper verbs in `tests/e2e/lib/steps.ts`.
#
# Tag filtering:
#   npx playwright test --grep @priority:critical
#   npx playwright test --grep @journey:contact
#   npx playwright test --grep @smoke

Feature: EsWikipedia — contact journey
  As a visitor of https://es.wikipedia.org/wiki/Madrid
  I want to complete the contact flow
  So that the page delivers on its user goal

  @journey:contact @priority:critical @smoke
  Scenario: contact journey reaches its terminal page
    Given I open the landing page
    And the page title contains "Madrid - Wikipedia, la enciclopedia libre"
    And the main heading reads "Madrid"
    When I navigate directly to "/wiki/Wikipedia:Contacto"
    Then I see the heading "Wikipedia:Contacto"
    And the page title contains "Wikipedia:Contacto - Wikipedia, la enciclopedia libre"
    # Form fill + submit (positive path)
    And I submit the form
    Then no error message is shown in the form region

  @journey:contact @priority:critical @kind:param
  Scenario Outline: contact — search accepts <variant> values
    Given I open the landing page
    When I enter "<value>" into the "search" field
    When I submit the form
    Then no error message is shown in the form region

    Examples:
      | variant   | value                                |
      | short | sample |
      | with-spaces | sample text with spaces |
      | unicode | café-niño-ümlaut |
      | emoji | hello 🎉 world 🌍 |
      | rtl-mark | test ‏rtl‎ content |
      | zero-width | sample​‌‍text |
      | with-quotes | she said "hello" & 'goodbye' |

  @journey:contact @priority:critical @kind:boundary
  Scenario: contact — long input does not break the form
    Given I open the landing page
    When I enter "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" into the "search" field
    Then no error message is shown in the form region

  @journey:contact @priority:critical @kind:tab-order
  Scenario: contact — tab order reaches the search field
    Given I open the landing page
    When I focus the "search" field
    When I enter "query" into the "search" field
    Then the "search" field has the value "query"

  @journey:contact @priority:critical @kind:empty-state
  Scenario: contact — page renders the landing list
    Given I open the landing page
    Then the page has at least 1 items

  @journey:contact @priority:critical @kind:resume
  Scenario: contact — deep-link to the terminal page renders correctly
    Given I open the page "/wiki/Wikipedia:Contacto"
    Then I see the heading "Wikipedia:Contacto"

  @journey:contact @priority:critical @kind:back-button
  Scenario: contact — back button after navigation returns to landing
    Given I open the landing page
    When I click the link to "/wiki/Wikipedia:Contacto"
    When I go back in the browser history
    Then the main heading reads "Madrid"

