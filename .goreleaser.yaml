version: 2

project_name: reviewqa

before:
  hooks:
    - go mod tidy

builds:
  - id: reviewqa
    main: ./cmd/reviewqa
    binary: reviewqa
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: reviewqa
    name_template: "reviewqa_{{ .Os }}_{{ .Arch }}"
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-dev"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^test:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: spriteCloud
    name: reviewqa
  draft: false
  prerelease: auto
  name_template: "{{ .ProjectName }} {{ .Version }}"
