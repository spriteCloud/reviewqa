{
  "name": "quail-landing",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page for the quail GitHub Action.",
  "scripts": {
    "serve": "npx --yes http-server . -p 4173 -s",
    "test": "playwright test",
    "test:prod": "PROD_URL=https://spritecloud.github.io/quail/ playwright test",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
